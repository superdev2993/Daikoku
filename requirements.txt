# Python dependencies for Daikoku
# Tested with Python 3.11 and PyTorch 2.4.1+cu124
# Install PyTorch separately first (see Usage.md)

# Deep Learning - Mamba architecture (optional: GPU acceleration)
# Without these, the system uses a pure PyTorch CPU fallback (extremely slower for training)
# pip install mamba-ssm==2.3.0 causal-conv1d==1.5.2

# Time Series Analysis
scipy==1.17.0

# Data Processing
pandas==3.0.0
numpy==2.4.1
numba==0.64.0

# Machine Learning
scikit-learn==1.8.0

# Hyperparameter Optimization
optuna==4.7.0

# Visualization and Logging
# setuptools<81 required: tensorboard 2.20 uses pkg_resources (removed in setuptools 82+)
setuptools>=65,<81
tensorboard==2.20.0
matplotlib==3.10.8
plotly==6.5.2
tqdm==4.67.1

# Exchange API
ccxt==4.5.36

# Date utilities
python-dateutil==2.9.0

# Testing
pytest>=9.0.0
pytest-cov>=7.0.0
